/**
 * Shared Vendor Dependencies
 * Common libraries used by both admin and presentation
 */

// Core AngularJS
window.angular = require('angular');

// jQuery (required by Angular Material)
window.jQuery = window.$ = require('jquery');

// Shared utilities
require('angular-animate');

console.log('📦 Shared vendor libraries loaded');
